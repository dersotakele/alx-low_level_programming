#include "holberton.h"

/**
*more_numbers - print 10 times the numbers
*
*
*/
void more_numbers(void)
{
	int x;
	int y;

	for (x = 0; y <= 9; x++)
	{
		for (x = 0; y <= 14; y++)
		{
			if (y > 9)
				_putchar((b / 10) + '0');
			_putchar((y % 10) + '0');
		}
	_putchar('\n');
	}
}
